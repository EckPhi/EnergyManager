[tool.poetry]
name = "energy-scheduler-connectors"
version = "0.1.0"
description = "Device I/O connectors for the Energy Scheduler"
authors = ["EnergyManager Contributors"]
license = "Apache-2.0"
packages = [{ include = "energy_scheduler/connectors", from = "src" }]

[tool.poetry.dependencies]
python = "^3.12"
httpx = "^0.28"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
